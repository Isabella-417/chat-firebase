export type ButtonProps = {
  initialColor: string
  finalColor: string
  icon?: string
  text?: string
  thumbnailColor: string
  onClick: () => void
}

export type ThumbNailProps = {
  thumbnailColor: string
}
